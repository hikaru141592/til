# til/2025-10-01.md

## 学習内容1
- **タグ**: Ruby, コーディング試験対策
- **タイトル**:メモ
- **内容**:
  - getsを使用する際、chompメソッドを用いることで行末の改行コードを取り除ける
    - getsで「Free」という文字列のみの行を取得したつもりだったが、`"Free"`と一致しなかった。
  - 配列から特定の条件を満たす要素を削除するには以下の例ようにrejectメソッドを用いる
    ```
    result = arr.reject{|x| x.include?("ABC")}
    ```
  - 配列から重複要素を削除するにはuniqメソッドを使う
