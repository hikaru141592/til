# til/2025-11-13.md

### 学習内容 1

- **タイトル**: MVC モデルとは
- **内容**:
  - プログラムを役割ごとに Model・View・Controller の 3 つに分けて管理するソフトウェア設計モデルのこと。役割ごとにコードを分割することでシステムの保守性や生産性の向上を図る。
    - M：ビジネスロジックを担当する部分。DB とデータをやり取り（データの登録・更新・削除）を行う。
    - V：ユーザーが実際に見る画面。
    - C：Model と View の制御を担当する部分。

### 学習内容 2

- **タイトル**: その他
- **内容**:
  - Dcocker を使用したアプリケーションの DB に接続際する際は、docker コマンドを使用し DB コンテナを指定し DB 接続する。
  - Rspec において、it の外で create するとテスト終了後もデータが残る。
  - テストはまず日本語だけで表現してみる。

  it を fit にするとそこだけテストする
  binding.irb でテストを止める、コンソールになるのでデータの中身見たり可能

  最後の let の後には空行を入れる。
  let と let!の違い
  before do と let!の違い
  Rubocop の検査対象外
  "spec/**/\*"　の**はディレクトリを再帰的に表現している
  VPC
  docker compose exec rails rails g model Category name:string description:text deleted_at:datetime
  docker compose exec rails rails g migration CreateAuthor first_name:string last_name:string biography:text birth_date:date deleted_at:datetime
  docker compose exec rails rails g model Author first_name:string last_name:string biography:text birth_date:date deleted_at:datetime
  docker compose exec rails rails g model Publisher name:string website:string phone:string address:string deleted_at:datetime
  docker compose exec rails rails g model Book name:string description:text isbn:string published_date:date total_copies:integer available_copies:integer category:references publisher:references deleted_at:datetime

user:references

    references category FK "NOT NULL"
    references publisher FK "NOT NULL"
    timestamp deleted_at "論理削除"
    timestamp created_at
    timestamp updated_at

books {
bigint id PK
varchar name "NOT NULL"
text description
varchar isbn "UNIQUE"
date published_date
int total_copies "NOT NULL, DEFAULT 1"
int available_copies "NOT NULL, DEFAULT 1"
references category FK "NOT NULL"
references publisher FK "NOT NULL"
timestamp deleted_at "論理削除"
timestamp created_at
timestamp updated_at
}

# til/2025-11-14.md

### 学習内容 1

- **タイトル**: trait について
- **内容**:
  - プログラムを

同じ変数名を内側のコンテキストで再定義すると可読性が下がり
NOT NULL でない場合も nil や空文字列が有効であることを確認すべき
category2 という雑な命名ではなくて duplicate_category とか適切な命名を
重複テストでは case_sensitive の確認をすべき,Rails6 以降は uniqueness バリデータは大文字小文字を区別しない、区別させるには case_sensitive を用いる。
cursor ターミナル　行選択？　 irb コンソール
datetime,timestamp
merge 先
マイグレーションファイル作成の際カラムごとにコメントを作成可能

一部の参照のプッシュに失敗しました
ヒント: リモートにローカルにない作業が含まれているため、更新が拒否されました。
ヒント: これは通常、別のリポジトリが同じ参照にプッシュしたことが原因です。
ヒント: リモートの変更を統合したい場合は、再度プッシュする前に 'git pull' を使用してください。
ヒント: 詳細は 'git push --help' の 'fast-forward に関する注意'(Note about fast-forwards) を参照してください。
hint: preference for all repositories. You can also pass --rebase, --no-rebase,
hint: or --ff-only on the command line to override the configured

現在の時刻
Time.now
Time.current
今日の日付
Date.today は、実行環境のタイムゾーンにおける当日の日付を取得する。
Time.zone.today は、実行環境によらず Rails の config.time_zone に設定されたタイムゾーンにおける当日を取得する。
→ 特別な理由がなければ Time.zone.today を利用した方が安全
Date.today の利用が問題となる例
ある Rails プロジェクトで config.time_zone には Asia/Tokyo のタイムゾーンが設定されているが、サーバーの環境変数 TZ は指定しておらず、タイムゾーンが UTC となっている。
その場合、当日の日付を取得する意図で Date.today を使うと、00:00〜8:59 の間は、当日ではなく前日の日付が得られ、意図しない挙動になり得る。
→ 実際に、CI 上で TZ が指定されておらず、特定の時間帯だけ Date.today を利用しているテストが落ちるという現象に遭遇した。

エラーメッセージの追加の仕方について、Rails6 以前と 7 以降で異なる模様

rails-training-itou(dev):006> c.errors.full_messages
=> ["Birth date 日付が古すぎます。適切な日付を入力してください。"]
rails-training-itou(dev):007> c.errors.details[:birth_date]
=> [{error: "日付が古すぎます。適切な日付を入力してください。"}]

エラーメッセージの最初に属性（カラム名）が表示されるのは、rails の仕様
errors.add(:属性名, "エラーメッセージ")
で指定すると、エラーメッセージの先頭に属性名が表示される。
errors[:base] << "モデル全体に関係するエラーを追加"
で指定するとモデル全体に関係するエラーを追加することになり、
また、先頭に属性名が表示されることもない。

ロケールファイルの分割について
テストは可読性重視、ループや if 文は使わない
ruby の挙動でわからないことがあったら irb で確認する

{ name: "伊藤" }は{ :name => "伊藤" }の省略形
name = :first_name のとき、
{ name: "伊藤" }
=> {name: "伊藤"}
だが
{ name => "伊藤" }
=> {first_name: "伊藤"}
となる

エラーメッセージや errors.added?のソースコードを確認したところヒントになるような記述がありました

git stash -u
コミットしていない変更がある状態で上記のコマンドを実行すると、変更した部分が退避されます。
ワーキングディレクトリ上は差分がない状態になります。
「コミットしていない変更」とは、add したものも add していないものもどちらも含まれます。
-u は　--include-untracked の略です。新規作成ファイル(追跡対象に含まれていないファイル)も退避することができます。
travel to
Proc
思い込み
実装やエラーの解決でつまづ撒いた際は「何をしたくて、どのようなことを試して、どのような結果が得られたか」アウトプットし自身で客観視し聖知りして扱えるようにする。

【処理済みに変更】（研修生）
approve ありがとうございました。
PR をマージしました。
PR: https://github.com/.../pull/123

【フィードバック後、レビュー中に変更】（研修生）
ご指摘いただいた点を修正しました：

- ○○ の処理を △△ に変更（コミット: abc123）
- テストケースを追加（コミット: def456）
  再レビューをお願いします。

[やりたいこと]
comparison を用いて「現在の日付」までを適切範囲内とするバリデーションをテストで読み込み、さらに to_travel を用いて「現在の日付」を指定した際に、バリデーションにおける「現在の日付」を to_travel の指定の日時となるよう反映させたい。

[やってみたこと]

- バリデーションを以下のように設定。

```
validates :birth_date, comparison: { greater_than_or_equal_to: OLDEST_DATE, less_than_or_equal_to: Time.zone.today }, allow_nil: true
```

- テストを以下のように記述

```
context '明日の場合' do
  let(:birthday_on_tomorrow) { build(:author, birth_date: Date.new(2025, 1, 2)) }

  it '無効' do
    travel_to Date.new(2025, 1, 1)
    expect(birthday_on_tomorrow).to be_invalid
    expect(birthday_on_tomorrow.errors).to be_added(:birth_date, :less_than_or_equal_to, count: Date.new(2025, 1, 1), value: Date.new(2025, 1, 2))
  end
end
```

- テストを実行したところ以下のようなエラーに直面。

```
Failures:

  1) Author birth_dateカラムのバリデーションテスト 日付範囲に関するテスト 明日の場合 無効
     Failure/Error: expect(birthday_on_tomorrow).to be_invalid
       expected `#<Author id: nil, biography: "ダミーです。", birth_date: "2025-01-02", created_at: nil, deleted_at: nil, first_name: "佐藤", last_name: "太郎", updated_at: nil>.invalid?` to be truthy, got false
     # ./spec/models/author_spec.rb:261:in 'block (5 levels) in <top (required)>'

Finished in 0.14751 seconds (files took 0.35758 seconds to load)
49 examples, 1 failure

Failed examples:

rspec ./spec/models/author_spec.rb:259 # Author birth_dateカラムのバリデーションテスト 日付範囲に関するテスト 明日の場合 無効
```

- binding.irb を用いて以下を確認。
  - 生成された`birthday_on_tomorrow`の`birth_date`が期待通り 2025/1/2 で作成されている。
  - travel_to の直後で現在の日付を取得すると期待通り 2025/01/01 になっている。
  - `expect(birthday_on_tomorrow).to be_invalid`を`be_valid`にし、その後ろの`expect(・・・`をコメントアウトするとテストが成功
- バリデーションの`Time.zone.today`を`Proc.new { Time.zone.today }`に置き換えたところ従来のテストコードが通るように。

[現状]

- （バリデーションの`Time.zone.today`を`Proc.new { Time.zone.today }`に置き換えない場合）テスト実行時の日付を travel_to で固定できているが、バリデーションの`Time.zone.today`はその日付を参照していない。
- （バリデーションの`Time.zone.today`を`Proc.new { Time.zone.today }`に置き換えた場合）バリデーションの`Proc.new { Time.zone.today }`は travel_to で固定した日付参照する。
  [質問/わからないこと]
- `Time.zone.today`を`Proc.new { Time.zone.today }`とするとなぜバリデーションの「現在の日付」が travel_to 指定のものを参照するようになるのか。

[参考]
https://qiita.com/scivola/items/33b962a497968a4fee5a

validates :phone, length: { maximum: PHONE_MAX_LENGTH}, format: { with: /\A[0-9\-]+\z/, message: "数字とハイフンのみが使えます" }
バリデーションエラーのデフォルトのメッセージを i18 化する

docker compose exec rails bin/rails c → Rails プロジェクト固有の環境として実行
docker compose exec rails rails c → PC の環境として実行
docker compose exec -it rails(もしくはコンテナ ID) /bin/bash でコンテナに入ってから、rails c 　 → Rails プロジェクト固有の環境として実行
(Rails はプロジェクトルートの bin ディレクトリにある rails ファイル（実行可能ファイル）を優先的に起動する仕様になっているからです。
よって、bin/rails（実行可能ファイル）が Spring を使うようになっていれば、bin/ を付けなくても Spring が起動することになります。)

s ３は置いておくだけ、レスポンスは悪い

正規表現について、論理積は難しい

- nil：データ自体が存在しない
- empty：配列自体は存在するが要素が存在しない 空文字も
- blank:nil と empty の和集合
- present: blank の補集合
  URI.parse

正規表現　キャプチャグループ
Ruby の正規表現では`^`と`$`はあくまで「行頭」と「行末」を意味するメタ文字であって、「文字列の先頭」と「文字列の末尾」を意味するメタ文字ではない。
`\A`と`\z`はそれぞれ「文字列の先頭」と「文字列の末尾」を意味する。
numericality オプション
trait を使って関連モデルのファクトリーボットを生成

16 時になったらカーテン！！！！！！！！！！！！！
16 時になったらカーテン！！！！！！！！！！！！！
16 時になったらカーテン！！！！！！！！！！！！！
16 時になったらカーテン！！！！！！！！！！！！！
16 時になったらカーテン！！！！！！！！！！！！！
16 時になったらカーテン！！！！！！！！！！！！！
16 時になったらカーテン！！！！！！！！！！！！！
16 時になったらカーテン！！！！！！！！！！！！！

validates :total_copies, presence: true, numericality: { only_integer: true, greater_than_or_equal_to: :available_copies }

[やりたいこと]

```
validates :total_copies, presence: true, numericality: { only_integer: true, greater_than_or_equal_to: :available_copies }
```

このバリデーションを設定したカラムをモデルテストの存在性の検証で、

```
    context '総部数が存在しない場合' do
      let(:book_without_total_copies) { build(:book, total_copies: nil) }

      it '無効' do
        expect(book_without_total_copies).to be_invalid
        expect(book_without_total_copies.errors).to be_added(:total_copies, :blank)
      end
    end
```

のように「nil ならバリデーションエラーが出る」といった結果を期待しテスト実行すると、

```
Failures:

  1) Book 存在性に関するテスト 総部数が存在しない場合 無効
     Failure/Error: expect(book).to be_invalid

     TypeError:
       can't convert nil into Float
     # ./spec/models/book_spec.rb:54:in 'block (4 levels) in <main>'

Finished in 0.01851 seconds (files took 0.3699 seconds to load)
1 example, 1 failure

Failed examples:

rspec ./spec/models/book_spec.rb:52 # Book 存在性に関するテスト 総部数が存在しない場合 無効
```

といったエラーが出ます。
このエラーが出ないようにし期待通りのテスト結果を出したいです。

[やってみたこと]

- バリデーションの`numericality: { only_integer: true, greater_than_or_equal_to: :available_copies }`を削除するとエラーは解消。このことから「numericality の処理内で比較に使う数値を float に変換しようとしてるため、nil が変換できずエラーが出てる」と推測。

```

[現状]
- 「numericalityの処理内で比較に使う数値をfloatに変換しようとしてるため、nilが変換できずエラーが出てる」と考えられる。
[質問/わからないこと]
- total_copiesがnilのときに`numericality: { only_integer: true, greater_than_or_equal_to: :available_copies }`を通すにはどうすればいいかがわかりません。
- 存在性のテストを省略することは可能なのでしょうか。

      name: 'a', isbn_code: '9-9999-9999-9', total_copies: nil, available_copies: 1, publisher: 4


belongs_to :editor, optional: true
一つのValidatesのうち一部のバリデーションを条件付きとしたい場合、二つのValidatesに分ける。
中間テーブル
uniqueness: { scope
複合ユニークキー

・has_many :XXX 等によるモデル関連付けの際に使うdependentオプションとは、モデルのレコードがdestroyされたときの、関係付けされたモデルに対する挙動を定義するもの。
　・:destroy 親と一緒に子レコードも削除
　・:delete_all 親と一緒に子レコードも削除、ただし直接DBのレコードを削除するので、子レコードのbefore_destroy等のコールバックコールバック処理は実行されない
　・:nullify 子レコードの外部キーを NULL 更新
　・:restrict_with_exception 子レコードがある場合は ActiveRecord::DeleteRestrictionError が発生
　・:restrict_with_error 子レコードがある場合は削除できず、親レコードにエラー情報が付加
basenameとextname
ActiveStorageの属性に対しバリデーションをかける
content_type
chrome　コマンドT、コマンドWでタブ開いたり閉じたり、/で検索へ

StringIO
ブラインドタッチ
book.thumb.blobでthumbのカラム確認
StringIO.new("hogeee", 'r+')
理解する時は読むだけでなく実際に実行して試す


ActiveRecordの役割は、(1)DBのテーブルをRubyのクラスとして扱える、(2)SQLを直接書かなくてもRubyのメソッドでDB操作ができる、DBの種類（MySQL、PostgreSQL など）を意識せずに開発できる
.rubocop.ymlの設定が効かない→rubocop-omakaseが無効化してる可能性→Enabled: trueで有効化→それでもダメならStyle:でEnabled: true
%記法について
Intクラスで使える.kilobyteメソッドや.megabyteメソッドなどについて
ActiveStrageとSQLの使い分け
N＋1問題の解決に使われるeager_load, preload, includesの使い分けについて


ruby
もともとは「Perl」と呼ばれる、Web開発によく利用される言語が由来となっています。
Perlと同様にWeb開発を得意領域としていますが、ほかにもスマホアプリ開発やWebデータ抽出を意味する「スクレイピング」など、幅広い用途に使われています。
「Perlよりも強力で、Python2よりもオブジェクト指向な言語が欲しかったんだ2」
Rubyは わざと 単一継承しか備えていません。
Rubyという言語の特性上、記述がかなり自由です。Javaでやろうと思ったら三億年くらいかかるコードが、Rubyだと瞬殺できたりします。

```

with_lock でトランザクションを作成して背後のレコードにロックをかける。

コールバックチェインの途中で例外が発生した場合は、ActiveRecord::Rollback または ActiveRecord::RecordInvalid 例外でない限り、Rails は例外を再度 raise します。代わりに throw :abort を用いてコールバックチェインを意図的に停止する必要があります。いずれかのコールバックが:abort をスローすると、プロセスは中止し、create は false を返します。
find_or_initialize_by と find_or_create_by の違いは
新規作成して保存する or しない

ruby は動的型付言語なので、型が変わるからと言って新しい変数を用意する必要はない、むしろ無闇に変数を増やさないことが好まれる
binding.irb は Rspec に限らず使用可能、複数の binding.irb が挟まっている等で処理を途中で終了させたい場合は exit!が有効

<%= image_tag "main/login.png",
alt: "ログインバナー",
class: "img-fluid mb-4 login-banner",
width: 1114, height: 707,
loading: "eager", decoding: "async", fetchpriority: "high",
style: "z-index: 0;" %>

        <body style="display: flex; フレックスボックス作成これは複数の要素とそれをラップするコンテナから構成される
              flex-direction: column;　rowは横並び、columnは縦並び、-reverseをつけると順序が逆に
              min-height: 95vh;">

フレックスボックス（フレキシブルボックス）とは

rails routes でルーティング確認

Rails のルート設計は RESTful です
resources メソッド
ストロングパラメータについて
Rspec において、#XXX はインスタンスメソッドを表し、/はパスを示す。
config.application.rb はアプリ全体へ影響させる必要のある設定にとどめ、他は環境ごとの設定ファイルに記述する。

private
def book_params
params.require(:book).permit(:name, :description, :isbn_code, :published_date, :total_copies, :available_copies, :category, :publisher)
end

require 'rails_helper'

RSpec.describe "Books", type: :request do
describe "GET #index" do
it "returns http success" do
get books_path
expect(response).to have_http_status(:success)
end

    it "レスポンスボディに含まれるべき内容が含まれる" do
      get books_path
      expect(response.body).to include("書籍一覧")
    end

end

describe "GET #show" do
it "returns http success" do
get book_path(Book.first.id)
expect(response).to have_http_status(:success)
end

    it "returns http success" do
      get book_path(Book.last.id + 1)
      expect(response).to have_http_status(404)
    end

end
end

scss と css の違い

| 比較項目                           | **Ruby**                               | **PHP**                          | **Java**                                 |
| ---------------------------------- | -------------------------------------- | -------------------------------- | ---------------------------------------- |
| **オブジェクト指向の純度**         | primitive 型含めすべてがオブジェクト。 | PHP5 以降で導入                  | 厳密・体系的な OOP                       |
| **OOP の特徴**                     | 柔軟・表現力高い・メタプロが強力       | 実用的・トレイトで多重継承風実現 | カプセル化・継承・ポリモーフィズムが厳密 |
| **型付け**                         | 動的型付け                             | 動的型付け（型宣言も可能）       | 静的型付け                               |
| **動的/静的その影響**              | 柔軟で開発が速い                       | 柔軟＋段階的に型補強できる       | 安全・堅牢・IDE サポート強力             |
| **文法の読みやすさ**               | ◎ 直感的で自然言語に近い               | ○ モダン PHP は読みやすい        | △ 明確だが冗長になりがち                 |
| **文法の書きやすさ**               | ◎ 少ない記述で表現できる               | ○ 実務に合わせて書きやすい       | △ 記述量が多い                           |
| **標準ライブラリの特徴**           | 人間に優しい設計。直感的               | 歴史的に統一性が弱いが改善中     | 非常に豊富で大規模向き                   |
| **パフォーマンス**                 | △ 遅めだが改善中                       | ○ PHP7 以降高速化。JIT 有        | ◎ JVM による高速実行                     |
| **実行環境/処理系**                | MRI・JRuby など                        | Web サーバー環境に強い           | JVM でどこでも動く                       |
| **用途の向き不向き（言語レベル）** | Web/スクリプトに強い                   | Web 全般に強い                   | 大規模・高信頼性に強い                   |
| **初心者の学びやすさ**             | ◎ 非常に入りやすい                     | ○ Web 目的なら比較的簡単         | △ 文法が厳密で難度高め                   |
| **企業での利用規模**               | 中小〜スタートアップ                   | 中小〜中規模                     | 大企業・金融・官公庁など圧倒的           |
| **柔軟性**                         | ◎ とても高い                           | ○ ほどほど                       | △ 型の制約で低め                         |
| **堅牢性**                         | ○（テスト重視で担保）                  | ○（改善が進む）                  | ◎ 型と設計で非常に高い                   |
| **言語思想まとめ**                 | “人間に優しい純粋 OOP”                 | “実務的な OOP を備えた Web 言語” | “厳密で堅牢な王道 OOP”                   |

| 観点                       | **Ruby**                                                     | **Java**                                                       |
| -------------------------- | ------------------------------------------------------------ | -------------------------------------------------------------- |
| **オブジェクト指向の性格** | **最も純粋な OOP**（ほぼ全てがオブジェクト）                 | **最も厳密な OOP**（厳格なルールに従う）                       |
| **値の扱い**               | 数値・文字列・true/false・nil・クラスすらも全部オブジェクト  | `int` などのプリミティブ型はオブジェクトではない               |
| **型システム**             | 動的型付け：変数の型を気にせず自由に扱える                   | 静的型付け：型宣言必須で厳密にチェック                         |
| **クラスと構造**           | クラスを書かなくてもコードが動く／クラスを途中から再定義可能 | すべてのコードはクラスの中に書く必要がある                     |
| **継承**                   | 単一継承＋ module（ミックスイン）で柔軟に拡張可能            | 単一継承＋ interface で拡張（ルール厳密）                      |
| **メソッド**               | すべてがメソッド呼び出しとして統一されている                 | プリミティブにはメソッド不可。ラッパー必要                     |
| **設計ルール**             | ルールが柔らかく自由度が高い                                 | アクセス修飾子・抽象クラス・インターフェースなどのルールが厳密 |
| **柔軟性**                 | とても高い。「自然に書ける」                                 | 低め。「正しく書かないと動かない」                             |
| **学習のしやすさ**         | 実行して動きを確かめやすい                                   | 厳格ゆえに習得にややステップが必要                             |
| **大規模開発との相性**     | 柔軟で小〜中規模に強い                                       | 厳密さゆえに大規模開発に特に強い                               |

| 観点                       | **Ruby**                                 | **Java**                                                   | **だから何？（意味・効果）**                                                               |
| -------------------------- | ---------------------------------------- | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| **オブジェクト指向の性格** | 最も純粋（ほぼ全部がオブジェクト）       | 最も厳密（OOP ルールが強制）                               | Ruby は“直感的で美しいコード”になりやすい。Java は“ルールが揃った堅牢な設計”になりやすい。 |
| **値の扱い**               | 数値・nil・true/false もオブジェクト     | プリミティブ型とオブジェクトが分かれている                 | Ruby は統一的に扱えて楽。Java は性能面や厳密性を重視できる。                               |
| **型システム**             | 動的型付け（柔軟）                       | 静的型付け（厳密）                                         | Ruby は試し書き・素早い開発が得意。Java はコンパイル段階で多くのエラーを防げる。           |
| **クラス構造**             | クラス無しでも動く／再オープン可能で柔軟 | 全てクラスの中に書く必要がある                             | Ruby は小規模で柔軟な開発に強い。Java は規律のある構造で大規模管理がしやすい。             |
| **継承・拡張**             | 単一継承＋ module で自由度高い           | 単一継承＋ interface で明確に分離                          | Ruby は実装がラク。Java は大規模でも衝突しにくい構造になる。                               |
| **メソッドの扱い**         | なんでもメソッド呼び出し                 | プリミティブはメソッド不可                                 | Ruby は統一感が高く理解しやすい。Java は軽量なプリミティブで高速処理が可能。               |
| **設計ルール（厳しさ）**   | ルールがゆるく柔軟                       | アクセス修飾子・抽象クラス・インターフェースのルールが厳密 | Ruby は“簡単に動く”が、Java は“設計を破綻させにくい”。                                     |
| **柔軟性**                 | とても高い                               | 比較的低い（設計ありき）                                   | Ruby は素早く変更可能。Java は安定重視。                                                   |
| **学習コスト**             | 低め／直感的                             | 型・構造の理解が必要でやや高い                             | Ruby は初心者向き。Java は基礎力をしっかり育てられる。                                     |
| **大規模開発適性**         | 小〜中規模向き                           | 大規模向き                                                 | Ruby はスピード重視の開発に最適。Java は企業システムなど長期間運用に強い。                 |

| 観点                     | オブジェクト指向言語（例：Ruby / Java / Python）       | 非オブジェクト指向言語（例：C / COBOL / Fortran） |
| ------------------------ | ------------------------------------------------------ | ------------------------------------------------- |
| **基本概念**             | オブジェクト（データ＋振る舞い）を中心として設計       | 手続き（処理の順番）を中心として設計              |
| **コードの再利用性**     | 高い（クラス継承、ポリモーフィズム、カプセル化が強力） | 低め（関数の再利用は可能だが設計思想が異なる）    |
| **拡張性・保守性**       | 高い。変更に強く、大規模開発向き                       | 規模が大きいと複雑になり保守が難しくなりがち      |
| **学習コスト**           | やや高い（概念が多いため）                             | 比較的低い（処理を順番に書けば動く）              |
| **処理速度**             | オブジェクトの仕組みがある分、やや遅くなる場合も       | 速い場合が多い（低レイヤーに近い）                |
| **メモリ効率**           | 抽象化の分、オーバーヘッドが発生しやすい               | 良い（ハードウェアに近いレベルで制御可能）        |
| **大規模開発**           | 高い適性（役割分担・再利用・テストがしやすい）         | 適性は低め（複雑化しやすい）                      |
| **小規模スクリプト**     | やや重くなることも                                     | 適性が高い（シンプルでスピーディ）                |
| **用途**                 | Web アプリ、ゲーム、業務システムなど広い               | 組み込み、システム開発、高速処理分野など          |
| **メリット（まとめ）**   | ・保守しやすい                                         |                                                   |
| ・再利用性が高い         |                                                        |                                                   |
| ・設計が整理される       | ・高速で効率が良い                                     |                                                   |
| ・構造が単純で学びやすい |                                                        |                                                   |
| ・小規模な処理に向く     |                                                        |                                                   |
| **デメリット（まとめ）** | ・抽象化が多く学習コストが高い                         |                                                   |
| ・処理が重くなることも   | ・大規模開発では破綻しやすい                           |                                                   |
| ・再利用や拡張が難しい   |                                                        |                                                   |

大規模なソフトや複雑なシステムを作るならオブジェクト指向言語が有利
高速処理や小規模なプログラムなら非オブジェクト指向言語も魅力的

プリミティブ型は軽くて高速
オブジェクト型はメソッドを持てる

Java がトップレベルコードを許さない理由
① 言語の思想が「厳格な構造」にあるため

Java は当初から
大規模開発・チーム開発での保守性を最優先
という思想で作られました。

そのため…

どこに何が書いてあるか明確

必ずクラスに所属する

どんなコードも構造化されている

という 規律を守らせる設計 になっています。

Ruby のような“柔らかさ”よりも、
“秩序と一貫性”を重視しているわけです。

✔ 1. 大規模開発でも迷わない（構造が安定する）

Java プロジェクトはどのコードも
「どのクラスの、どのメソッドに書いてあるか」
が必ず決まっています。

→ 初めてプロジェクトに入った人でも迷いにくい
→ フォルダやファイル構造が乱れにくい
→ あらゆる処理の場所が“明示的”

✔ 2. 役割が明確で、責務分離が徹底しやすい

Java ではクラス分けが自然と強制されるため

モデル

サービス

コントローラ

値オブジェクト

ユーティリティ

…など、責任ごとにクラスを分ける設計がやりやすいのです。

Ruby だと自由すぎて、初心者はつい

class User
def super_long_method_with_everything # ここに全部詰めこむ…
end
end

のような“神クラス”を作ってしまいがちでございます。

Ruby
1995 年リリース、Windows95 が発表された年でインターネットが普及
インターネットの世界は PC1 台で完結するものではなく、複数の PC が繋がり合うことで成り立っています。そのため、より複雑な処理が求められるようになりました。
Rails 2004 年

1993 年頃から新しい言語の設計を始め、1995 年に Ruby の初期リリースを行う。Perl のテキスト処理能力や Python のオブジェクト指向、Lisp の柔軟性などを取り入れながら、日本文化的な感性も融合させたのが特徴。プログラマの幸せを第一に考え、人間が自然に読み書きできる文法を追求した。

gitignore の活用について
レビュー提出時は必ず github でファイル差分を一通り確認する

      t.string :name, null: false
      t.text :description
      t.string :isbn_code, null: false, index: { unique: true }
      t.date :published_date
      t.integer :total_copies, null: false, default: 1
      t.integer :available_copies, null: false, default: 1
      t.references :category, foreign_key: true
      t.references :publisher, null: false, foreign_key: true
      t.datetime :deleted_at

<%= f.collection_check_boxes(:author_ids, Author.all, :id, :first_name, :prompt => true) do |author| %>
<%= author.label do %>
<%= author.check_box %>
<%= author.text %>
<% end %>
<% end %>

<%= f.collection_check_boxes(:author_ids, Author.all, :id, :first_name, :prompt => true) do |author| %>
<%= author.label do %>
<%= author.check_box %>
<%= author.text %>
<% end %>
<% end %>

<%= f.collection_select(:author_ids, Author.all, :id, :first_name, :prompt => true)%>

<% 5.times do %>
<br>
<%= f.collection_select(:author_ids, Author.all, :id, :id_and_name_and_birth_year , :prompt => true)%>
<% end %>

    if params[:book][:author_ids].present?
      params[:book][:author_ids].uniq.each do |author_id|
        BookAuthor.create(book_id: @book.id, author_id: author_id) if author_id.present?
      end
    end

map メソッドと join メソッドを用いた文字列結合の仕方
map メソッドの省略形について

<%= f.collection_select(:author_ids, Author.all, :id, :id_and_name_and_birth_year , :prompt => true)%>
これだと 単一選択用のセレクトボックスになってしまい、
author_ids が 配列形式で送信されない。
Rails で has_many 関連の \*\_ids を送る場合は、multiple: true が必須

（1 つのセレクトで複数選択する場合）
<%= f.collection_select :author_ids,
Author.all,
:id,
:id_and_name_and_birth_year,
{},
{ multiple: true } %>

5.times do
f.collection_select(:author_ids, ...)
end
これは 全部同じ名前属性になるため、最後の 1 つしか送られん。

もし「5 つのセレクトボックスで配列として送りたい」なら、
次のように name 属性を明示的に配列化する必要が。

修正版（5 個のセレクトで配列送信）
erb
コードをコピーする
<% 5.times do %>
<br>
<%= select_tag 'book[author_ids][]',
options_from_collection_for_select(Author.all, :id, :id_and_name_and_birth_year),
include_blank: true %>
<% end %>
確認ポイント
デバッグ用に以下もおすすめ：

コードをコピーする
Rails.logger.debug params.inspect
もしくはブラウザの開発者ツール → Network → Form Data を見ると
実際に author_ids[] が送信されているか確認でき

まとめ
現在の問題は以下が原因：

collection_select に multiple: true がない

同じ name を使ってループしているため配列として送信されない

redirect_to と render の違い
ステータスコードの意味
logger.debug("XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX")

トランザクションブロック内の処理が例外を起こさない限りロールバック処理は行われない
ブロック内で rescue で例外をキャッチしてしまうとロールバックしない
トランザクションの外側で例外をキャッチする。ロールバックが発生したら例外は再度送出される、という性質を利用します。
Rails4 までは、Controller のテストは controller spec で書かれていたのですが、Rails5 からは「request spec」で書くことが推奨されています。

create_list メソッドは配列を返す。
author を create_list で作成し map メソッドを使うことで各 id を要素とした配列も作れそう。
return if だけでなく next if もガード句
Active Support の#presence メソッドを使う
expect().to include()の include には複数の引数を渡すことが可能
rubocop の警告を特定のテストのみ出させないようにすることが可能
{}のブロックの 2 重構造は良くない、do end を用いる。
has_many における関連付けで子のモデルの id 全てを取得できるメソッドが自動生成される
配列同士の和/積/浦集合を得るには、|、&、-を使う。

      author_ids.each do |author_id|
        book_author = BookAuthor.new(book_id: @book.id, author_id: author_id)
        logger.error('---start---')
        logger.error(book_author.book_id)
        logger.error(book_author.author_id)
        a = book_author.save
        logger.error(a)
        logger.error('---end---')
      end

params[:book][:thumb] は届いている
リクエストヘッダの Content-Type は multipart/form-data
@book.save!成功している
ActiveStorage::Blob と ActiveStorage::Attachment が作られない

bin/rails active_storage:install
bin/rails db:migrate

@book = Book.new(book_params)
@book.save!
@book.thumb.attach(params[:book][:thumb])
これで

ActiveStorage::Attachment.count
が 1 以上になれば。。。

attachments の insert 失敗を例外にしない？

`docker attach コンテナID`　でアタッチ
コンテナ ID は、`docker ps`で確認
docker attach は、起動中のコンテナのメインプロセスが利用する標準入出力に、現在のターミナルを接続する
ビューで直接モデルを参照するのはやめましょう
デバッグ用コードが残っている（本番環境に出すべきではない）、パラメータにはパスワードや個人情報など機密情報が含まれる可能性があり、ログに出力するとセキュリティリスクになる
assign_attributes メソッドで属性のハッシュを渡すことですべての属性を設定
ぼっち演算子

def update
book_author = nil
book_updated? = false
@book = Book.find(params[:id])

    ActiveRecord::Base.transaction do
      book_updated? = @book.update!(book_params)

      author_ids = params[:book][:author_ids] || []
      author_ids.reject!(&:blank?)

      delete_ids = @book.author_ids - author_ids
      create_ids = author_ids - @book.author_ids

      delete_ids.each do |author_id|
        next if author_id.blank?

        book_author = Book.find_by!(book_id: @book.id, author_id: author_id)
        book_author.destroy!
      end

      create_ids.each do |author_id|
        next if author_id.blank?

        book_author = BookAuthor.new(book_id: @book.id, author_id: author_id)
        book_author.save!
      end
    end

    redirect_to book_path(@book.id), notice: t('flash.books.edit.notice')

rescue ActiveRecord::RecordInvalid => e
logger.error('------------------------------------')
logger.error(e.message)
logger.error('------------------------------------')
@book_or_book_author = book_updated? ? book_author : @book
flash.now[:alert] = t('flash.books.edit.alert')
render :new, status: :unprocessable_entity
end

bundle install
rails turbo:install
rails importmap:install
turbo:install stimulus:install

ストロングパラメータについて、rails8 では expect が推奨される
レビューコメントに対する返事を pending のまま再レビュー依頼出さないこと
インスタンスメソッドを作成したらモデルスペックを書くこと
言われた箇所だけ修正するのではなくて、
他に同じようなところがないかも確認する癖をつけましょう。
レビュアーの意図を汲み取る動きができるとプロジェクト先でも重宝されるかなと！
git log でハッシュ値確認
git diff ハッシュ値
git revert (コミットのハッシュ値) で打ち消し（コミットを無かったことにするのではなく逆向きのコミット）
PUT と PATCH の主な違いは、**PUT がリソース全体を「置換（上書き）」**するのに対し、**PATCH はリソースの一部だけを「更新・修正」**する点

def destroy
book = Book.find(params[:id])
book.destroy!

    redirect_to books_path, notice: t('flash.books.delete.notice')

rescue ActiveRecord::RecordNotFound => e
logger.error(e)
logger.error(e.backtrace.join("\n"))

    redirect_to books_path, alert: t('flash.books.delete.alert')

end

    <%= link_to t('buttons.delete'), @destroy_url, data: {turbo_method: :delete, turbo_confirm: t('turbo_confirm.book_delete')}, class: 'btn' %>

ja:
app:
title: 書籍管理システム
buttons:
register: 登録
not_register: 登録をやめる
update: 更新
delete: 削除
links:
top: TOP へ戻る
turbo_confirm:
book_delete: 本当に書籍情報を削除しますか？

ja:
flash:
books:
show:
alert: 不正な URL です。
create:
notice: 書籍の登録に成功しました。
alert: 書籍の登録に失敗しました。
edit:
alert: 不正な URL です。
update:
notice: 書籍の更新に成功しました。
alert: 書籍の更新に失敗しました。
delete:
notice: 書籍の削除に成功しました。
alert: 存在しない書籍情報を削除しようとしたため、処理を中止しました。

# [https://github.com/hotwired/turbo-rails]

gem 'turbo-rails'

# [https://github.com/hotwired/stimulus-rails]

gem "stimulus-rails"

- 自分が取った対応
  - 実装機能に対しブラウザ操作をし、その操作で実装内容が問題ないと判断する
  - テストの作成をしエラーが出る
  - テストコードのどこでエラーが出てるか特定する
  - エラーの直前で必要なテストデータが正しく生成されていることを確認する
  - 実装内容が問題ないと判断していたため、テストコードのどこかに問題があると考えるが、その箇所が不明なため中島さんに相談する
  - 中島さんが実装内容の不備の可能性を指摘し実装内容の修正方法を提示するが、ブラウザ操作で問題が見られないという理由からその対応に納得がいかず、「① その修正がなぜ適しているか、② 開発環境における動作不備があるか」を聞く
    - ① については自分でも調べてみましたが、納得がついてなかったことから基本的に中島さんの説明を頼りにしていました。
  - 自分が納得するに至らず、その後中島さんが綿引さんに意見をいただく

- 見直すべき対応/考え方
  - ブラウザ動作で実装内容が問題ないと考えない
  - テストが失敗した時は、(まずテストがエラーになった箇所をできるだけ正確に特定し、その箇所でテストデータが正しく用意されているかを確認することは前提として)テストコードが問題なさそうであれば実装内容に不備があることを疑う
  - テストが失敗した時に、開発環境における動作不備の特定は必ずしも必要ではない。
    - テストでしかみつけられない不備を洗い出すことがテストの目的の一つと言えるため

Publisher.find(update_publisher.id).name,
Category.find(update_category.id).name,
\*default_book.authors[0..2].map(&:full_name)

ブラウザ動作だけで実装内容が問題ないと考え図、テストが失敗したら実装内容の不備も疑う
配列を引数に渡す際、その配列の前に\*を記述することで配列を展開して複数の引数として渡すことができる
each_with_index メソッドを配列に対して用いることで、ループ処理で配列の要素とカウンタ変数を利用することができる。

ステータスコードについて
ステータス 500 の時のエラーハンドリング
count メソッドは ActiveRecord で用いる場合通常の Ruby のメソッドとは挙動が異なる

throw によるループ脱出について
save や destroy は catch ブロックでラップされており、コールバックを用いた場合 catch を明示的に記さなくても throw を使うことができる。
bundle install の install はデフォルトオプションなので両者に差はない

add_index :posts, :discarded_at

save や update メソッドは内部的に with_lock の処理を走らせている
RSpec の before do は特定のテストケースでスキップさせることが可能、ただし let!は不可

git stash で退避させた差分の内容は git diff stash@{0}のようにして確認可能
rails8 標準の認証機能について
Rails の Delegate について（あるオブジェクト（一般的にはモデルオブジェクト）から別のオブジェクトへのメソッド呼び出しを委譲するために使用される）
Active Record の normalizes でモデルの属性の正規化が可能
Slack のブックマーク機能

```ruby
module Authentication
  #extend は、ある特定のオブジェクトだけにモジュールの機能を追加したいときに使用、includeと違いクラスメソッドとして読み込む
  extend ActiveSupport::Concern

  included do
    before_action :require_authentication #このクラスのインスタンスメソッド
    helper_method :authenticated? #このクラスのインスタンスメソッド、ヘルパーメソッドとしてビューで利用可能に
  end

  #このクラスメソッドは、require_authenticationコールバックで特定のアクションをバイパスするのに用います。
  class_methods do
    def allow_unauthenticated_access(**options)
      skip_before_action :require_authentication, **options
    end
  end

  private
    def authenticated?
      #現在のユーザーにアクティブなセッションがあるかどうかをチェックするヘルパーメソッドです。
      resume_session
    end

    #認証が必要なページの保護
    #セッションが見つからない場合は、ユーザーをログインページにリダイレクト。
    def require_authentication
      resume_session || request_authentication
    end

    #セッションを設定
    def resume_session
      Current.session ||= find_session_by_cookie
    end

    #cookieからセッションを復元
    def find_session_by_cookie
      Session.find_by(id: cookies.signed[:session_id]) if cookies.signed[:session_id]
    end

    #未認証ユーザーに対し、request.urlをセッションに保存しログインページにリダイレクトさせるために使う
    def request_authentication
      session[:return_to_after_authenticating] = request.url # アクセスもとURL
      redirect_to new_session_path
    end

    #認証後ユーザーに対し、ログイン後ページにリダイレクトさせるために使う
    def after_authentication_url
      session.delete(:return_to_after_authenticating) || root_url #認証後のリダイレクト先URLを返します。
    end

    #指定のユーザーのデバイスやIPアドレスの詳細情報を渡して新しいセッションを作成し、現在のセッションを設定
    def start_new_session_for(user)
      user.sessions.create!(user_agent: request.user_agent, ip_address: request.remote_ip).tap do |session|
        Current.session = session
        cookies.signed.permanent[:session_id] = { value: session.id, httponly: true, same_site: :lax }
      end
    end

    #現在のセッションを破棄して、cookieからセッショントークンを削除
    def terminate_session
      Current.session.destroy
      cookies.delete(:session_id)
    end
end
```

- ログイン/ログアウトの流れ
  - 初回ログイン
    - ユーザーのログイン情報や以前に行った選択などがセッションとしてサーバー側に保存される。
    - その情報の照合用としてセッション ID 情報を保持したクッキーをサーバーが発行し、ユーザーのブラウザが受け取る
      - 署名付きクッキーを使うと、クッキー内部の値を暗号化させることができ、改善の検知が可能になる
  - 2 回目以降ログイン
    - ユーザーがクッキーをヘッダーに含めたリクエストを送信
    - サーバー側でセッション ID の照合
    - ユーザーを判別しログイン状態が維持
  - ログアウト
    - サーバー側が該当セッション ID のセッションデータを破棄する。

- パスワードの管理(has_secure_password の場合)
  - ハッシュ化し、DB(の password_digest という属性)に保存
  - 検証の際は 2 つのペアの仮想的な属性（password と password_confirmation）を使う
  - 検証が OK の場合（authenticate メソッドによって）true を返し（その後 User オブジェクトを返す）、NG の場合 false を返す

HasSecureToken は、Ruby on Rails のあらゆるモデルに対して一意のランダムトークンを簡単に生成する

ユーザーがログイン状態を保持できる仕組みは？
パスワードはデータベースにどのように保存されている？（平文？暗号化？ハッシュ化？）
has_secure_password は内部で何をしている？
session と cookies の違いは？
cookies.signed.permanent の signed と permanent はそれぞれ何を意味する？
なぜセッションテーブルに IP アドレスと User Agent を保存するのか？
どういった OS やどういったブラウザから来たか

ハッシュ化と暗号化
ビジョン
bundle exec rails generate authentication

      t.string :email_address, null: false
      t.string :password_digest, null: false
      t.string :first_name, null: false
      t.string :last_name, null: false
      t.integer :role, null: false
      t.string :phone
      t.string :address
      t.datetime :deleted_at

      t.timestamps

## 概要

認証機能を実装するため、rails generate によるファイル自動生成とマイグレーションファイル編集までを行いました。

## Backlog 課題

[TRAINING-85](https://inations.backlog.com/view/TRAINING-85)
ステップ 21: 認証機能を実装してみよう

## 変更内容

- `bundle exec rails generate authentication`実行
- 自動生成されたファイルのうち、User テーブル作成のマイグレーションファイルを編集（カラム追加）
  - first_name
  - last_name, null: false
  - role, null: false
  - phone
  - address
  - deleted_at

## チェックリスト

- [ ] コードの目的が PR タイトルと一致している
- [ ] 不要なファイル・コメントが含まれていない
- [ ] コミットメッセージが適切である
- [ ] PR 本文の内容がチームメンバーに伝わる

## 補足

ユーザーは論理削除を適用する予定です。

Ruby には Email アドレス用の正規表現がライブラリとして利用できる
git stash でコメントを添えたりファイルを指定したりすることが可能

User.create!(
email_address: 'user1@example.com',
password: 'password',
password_confirmation: 'password',
first_name: '太郎',
last_name: '山田',
phone: '00-0000-0000',
address: 'サンプル県サンプル市サンプル区 0-0-0',
deleted_at: nil)

    Railsのdestroyアクションにstatus: :see_otherを付けるべきか否か
    同値と同一の違いについて、完全一致（正規表現）について
    ロケールファイルではUI上表示されないモデル名やカラム名についても設定をしておくべき

    ルーティングの記載順について
    htmlにおけるautocomplete
    開発環境でのメール送信、メール閲覧（letter_openerとletter_opener_web）について
    <br>タグで行間を設けレイアウト調整するのはアンチパターン（簡易的なデザイン作成の場合は使われることがある）
    スケジュールは細かく定めることでスケジュール遅延をすぐ共有できるようにし、また遅延理由を明確にする

！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！
！！！！！！！！！！！！！！16 時になる前にブラインド！！！！！！！！！！！！！！！！！！！
！！！！！！！！！！！！！！16 時になる前にブラインド！！！！！！！！！！！！！！！
！！！！！！！！！！！！！！16 時になる前にブラインド！！！！！！！！！！！！！！！！！
！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！

- letter_opener
  - まず、開発環境に gem を追加し、bundle コマンドを実行してインストールします。
    ```
    group :development do
      gem 'letter_opener'
      gem 'letter_opener_web', '~> 3.0'
    end
    ```
  - config/environments/development.rb
    ```
    config.action_mailer.delivery_method = :letter_opener_web
    config.action_mailer.perform_deliveries = true
    ```
  - routes.rb
    ```
    Your::Application.routes.draw do
      mount LetterOpenerWeb::Engine, at: "/letter_opener" if Rails.env.development?
    end
    ```
  - localhost:3000//letter_opener でアクセス

Postman と curl コマンドについて（詳しくは改めて時間ある時学習します。）
included do~end ブロックの中にメソッドを定義しておくとモジュールが include されたあとににそのメソッドが動作する。
extend ActiveSupport::Concern によりモジュールを include した時にインスタンスメソッドだけではなくクラスメソッドも取得できるようになる
StandardError を継承しないクラスのインスタンスを raise メソッドの引数に指定すると TypeError が発生する

Rspc における shared_examples や shared_context によるテストコードの共通化について
Rails のバリデーションエラーが発生した際のレイアウトが崩れの原因は<div class="field_with_errors">という HTML 要素で囲まれるため

Rails のバリデーションエラーが発生した際、デフォルトではエラーのあるフィールドが<div class="field_with_errors">という HTML 要素で囲まれるため、ラベルと入力欄が縦に並ぶなどレイアウトが崩れることがある
/_ field_with_errors の中の要素に余白を設定 _/
.field*with_errors label {
margin-right: 10px; /* ラベルの右側にスペースを追加 _/
display: inline-block; /_ 必要に応じてインラインブロック要素に変更 \_/
}

.field*with_errors input {
margin-bottom: 5px; /* 入力欄の下にスペースを追加 (エラーメッセージとの間隔) \_/
}

io = File.open('list.txt')

while not io.eof?
io.readlines
io.seek(0, IO::SEEK_CUR)
p io.readlines
end
新規ユーザー登録機能を実装してみよう

Rails の deliver_later と deliver_now の使い分け
ActionMailer によるメール送信のテストの書き方
Job を利用する際には専用のサーバーが別途必要、その用意がされていないならばバックグラウンド処理は行うことができない
ActionMailer を使用する際は from の設定もする
text.erb ファイルを扱う際は HTML タグを生成する記述をしないようにする

詰まった時は戻る
バリデーションのエラーは特定の:on オプションや if を使い特定の場合のみ適用させることが可能
assign_attributes メソッドについて
バリデーションの confirmation: true は confirmation の属性が nil の場合適用されない

user.assign_attributes(\*\*attr)
user.save(context: :update)

(29)
p "Hello%d" % 5

method_defined?

30

正規表現のメタ文字について復習
Rspec における shared_context

フォームでセレクトボックスを使用する
テストデータの作成で update を使用するとクエリが余計に発行されてしまう
JS のイベントについて（勉強会）
Rails のファットコントローラーとデザインパターンについて（勉強会）

Rspec の shared_examples と shared_context について、前者は it のブロックを共通化し、後者はそれの実行前のテストデータ準備等を共通化する（それらを一まとめにして共通化はできない）。
モデルにおける restrict_with_exception と restrict_with_error オプションについて
何年後などの日付操作

enum を定義することで利用できるヘルパーメソッドについて
grep コマンドと sed コマンドを組み合わせて文字列置換をする
find オプションの delete オプションで検索に引っかかったファイルを削除可能
どの行でエラーになってるかログに出力されている場合はそれを必ず確認すること
command + / で選択されたすべての行をコメントアウト

足し算や引き算でぼっち演算子を使うことはできない、(a || 0) + b のようにする

ruby で、 array = array1 + array \* 10 のようにすることで配列に同じ要素を複数格納可能
enum の値を取得すると返す値は文字列でありシンボルではない

Rspec コマンドでテストされる対象はファイル名末尾が\_spec.rb のもののみ（ファイル名指定した場合は任意のファイル名が許されてしまう）

RESTful なルーティングを既存のものからさらに追加する方法
link_to で任意のパラメータを渡す方法
モデルのインスタンスに対し、特定の属性を指定し値を更新する場合、createやupdateメソッドと異なり「モデルのidを示すカラム」をモデル名で指定したりモデルのインスタンスを代入することはできずidを指定する必要がある

スコープ外の作業をしないよう注意する、またその可能性がある場合はメンターに相談する。
カスタムアクションにおけるmemberとcollectionの違い

after_saveで保存後にバリデーションチェックをする
ブロックの中で早期returnを使うのではなくnextを使う
モデルの責務はできるだけ軽くする、現実でタスクを処理することをイメージするとモデルの切り分けがしやすい

changesメソッドでモデルのインスタンスの変更前の値と変更後の値を参照可能
モデルにはデータの変更前後の情報を取得するヘルパーメソッド（カラム名\_was等）が作成されている
バリデーションで弾かれるテストを書くときテストデータの作成はbuildにしないとエラーで落ちる

railsのrailsからは極力それない設計をすること
DoubleRenderErrorについて（特殊な条件下でも原則フォーム送信のエラー時はrenderを用いる）

findコマンドの使い方
カラムを削除するときのマイグレーションファイルの作成の仕方

grep -rl 'available_quantity' app config spec | xargs sed -i.bak -e 's/available_quantity/available_copies/g'

grep -rl '前' app | xargs sed -i.bak -e 's/前/後/g'
find . -name "アスタリスク.bak" -delete

33

puts "0b10".hex

2aiueokakikukekosasisusesotatituteto

2
?
4
2
3

26
5
?
?
4

-ioインスタンスメソッドについて

- readとreadlinesの違いは「一つのの文字列か配列か」
- getsとreadlineの違いは、ファイル末尾に達したときの反応で「nilを返すか例外を発生させるか」
- 優先順位
  - <<は||や&&や>などの論理演算より先
  - \*\*は負の数の-よりも優先、掛け算よりも優先
    - -5 \*_ 2 _ 2 = -50
  - 四則演算は<<より先
  - &,|,&&,||順に高い
  - &や||は==より高い
  - result = true and false はtrueが格納。andは=より優先度低い。=より低いのはandやorくらい
- 初期化されていない場合
  - nil：インスタンス変数、グローバル変数
  - 例外：クラス変数、定数、ローカル変数（＊）
    - ローカル変数は、実行されていないだけで代入分があるならnil
- pとputsとprint
  - pはデバッグ用
  - printだけ引数ごとの改行なし
  - putsやprintでもシングルクォーテーションだと改行等無効
- Array(3,2)はエラー
- Array(3)は[3]
- Array([3,2])は[[3,2]]ではなく[3,2]
- zipコマンドは非破壊だが破壊はなし
- ArrayとHash
  - delete
    - 以下共通
      - 引数一つ
      - !なしで破壊
      - 戻り値は削除した値
    - 違い
      - Array：番号ではなく値を指定、その値を全て消す
  - Arrayのto_hの二次元配列で対の要素が揃ってないとエラー
- キーワード引数
  - `def foo(a:, b:, c:400); end`のような気ワード引数を持つメソッドに`d: 600`のように存在しないキー渡すとエラー
  - `100, 200, 300`のようにキーワードなしで引数渡すのもエラー
- `**`をつけるやつは、受け取った引数を複数のキーワード引数を一つのハッシュにするのに使う
- Date.today.strftime("%~~~")
  - "%F": "2026-02-05"
  - "%x": "02/05/26"
  - "%D": "02/05/26"
- Date.to_s: "2026-02-05"
- procとlambda
  - 引数が足りない：nil、エラー
  - ブロックの中でreturn：その値を返す、ブロックをでた後の処理へ
- casecmpは大文字小文字を区別しない比較演算子と同等
  - "a".casecmp("b")で-1
- chompは末尾の改行コードを削除するが引数を与える対象が変わる（複数文字可能、ただし数字も文字列にすること
- delete
  - Stringのみ非破壊と破壊あり、Arrayやhashは!なしで破壊のみ
  - Stringのdeleteは引数("0-58-")のように範囲指定可能、引数複数でand条件可能、また重複全て消す（非破壊と破壊あり）
  - Arrayのdeleteは引数で一つの値しか指定できない、重複は全て消す（!なしで破壊しかない
- Time
  - オブジェクト生成
    - 現在
      - new,now（ローカル）
    - 時間指定（引数7個以下は年から引数渡す、10個は秒から）
      - mktime,local（ローカル）
      - gm,utc（UTC）
  - タイムゾーン変更
    - （ローカルに、UTCに）
    - 破壊：localtime、gmtime
    - 新オブジェクト生成：getlocal、getgmとgetutc
- dupとcloneの違い
  - cloneはfleezeを引き継ぐ
- freezeは破壊的変更は防ぐが再代入すると消える、というか変数に対してつくものではなくオブジェクトに対して使うもの
- %記法
  - %s シンボル単体
  - %iと%I シンボルの配列
- 破壊的メソッドは基本的に変更がない時はnilを返す
- appendとconcat
  - appendは配列のみ、concatはstringもarrayもある
  - 配列の場合
    - concatはarrayでは引数は配列のみ許容、配列同士の結合のため
    - appendは配列に要素追加のため引数がそのまま要素になる（配列が引数だと要素も配列に）
- octとかhexとかは変換できない場合はエラーじゃなく0
- ライブラリ
  - URI, JSON, YAML, CSV, Date, Digestが出てきたら、標準添付ライブラリ」なので、一番上に require '◯◯' が必要
  - Numeric, Timeなどは組み込みクラスなのでrequire不要
  - requireに加えて「includeか、URI:みたいなやつ」必要
    a[3, 1]: 長さを指定した切り出しの場合、「配列の末尾（この場合はインデックス3の位置）」ちょうどであれば、空の配列 [] を返すという特殊な仕様があります。
    もし a[4, 1] （末尾よりさらに外）であれば nil になります。
    Hash(a: 3)=> {a: 3}だけどHash()はエラー、Hash("a", 100)はエラー

h = Hash.new { |hash, key| hash[key] = "None" }
h[:a].upcase!
p h[:b]
はデフォルト値共有じゃないから更新されないよ

v = 1 + 2..3 + 4
p v.to_a
3から7

「include した先では private になる」 というのが module_function の大きな特徴の一つ

正規表現メタ文字

%記法について
カウンターキャッシュについて

Railsのassign_attributesメソッドについて（属性のハッシュを渡すことで複数の属性を設定）
linuxでのエスケープ
Rakeタスク

Solid Queue
仮想デスクトップ、arfred等について

dce rails rake remind_mail:send

検索フォームの入力内容に対しエスケープ処理をする方法、SQLにおけるワイルドカード
methodメソッドを用いてメソッド名が引数の文字列であるメソッドを呼び出す
Rspecにおけるdescribed_class
bootstrapの導入について










docker compose exec rails bin/rails dartsass:watch
docker compose exec rails bin/rails dartsass:build

@import 'bootstrap/scss/bootstrap';
@import 'bootstrap-icons/font/bootstrap-icons';

railsのフラッシュメッセージ対応は標準でalertとnoticeのみ、他のタイプを追加するにはadd_flash_typesメソッドを使う
CSSアニメーション（フェードアウト、アニメーションの待機など）
collection_selectの引数について
bootstrapのブレイクポイント

### Rubyオプション組み合わせマトリックス（-n × -e）

| | -e なし (ファイルを読み込む) | -e あり (コードを実行する) |
| :--- | :--- | :--- |
| **-n なし** (1回実行) | **【スクリプト実行】**<br>例：`ruby hello.rb`<br>内容：ファイルを1回実行。 | **【単発ワンライナー】**<br>例：`ruby -e 'puts "Hi"'`<br>内容：コードを1回実行。 |
| **-n あり** (全行ループ) | **【ループファイル実行】**<br>例：`ruby -n filter.rb data.txt`<br>内容：ファイルの内容で全行ループ。 | **【ループワンライナー】**<br>例：`ruby -ne 'p $_' data.txt`<br>内容：コードで全行ループ。 |

nをpにすると勝手にprintするようになる
lがあるとchompのように改行文字を取り除き、さらにprint時出力元に改行がなくてもそれぞれ改行するように

# test.txt の内容:
# apple
# banana
# cherry
ruby -ne 'p "Hi"' test.txt

# input.txt の内容:
# apple (末尾に改行あり)
ruby Fne 'p $_' input.txt


# data.txt の内容:
# 100 200 300
ruby -ane 'p $F[1]' data.txt

# カンマ区切りのデータに対して、正しく 2番目の値（"red"）を取り出すためのオプションはどれですか？
# list.txt の内容:
# apple,red,100

ruby XXX -ane 'p $F[1]' list.txt
aで二つ目取得（区切り文字半角スペース）
F区切り文字　で区切り文字変更

選択肢
-F,
-F:
指定不要

-Fの後は区切り文字が必要
-F,eのようにすると,eのに文字がセットで区切りになる
eの直後に書いたものはコマンドとして扱われる
ruby -ep\(3\)
3

一度どこかのクラスで @@val が定義されると、その子クラスで新しく定義したつもりでも、親の変数を上書きしてしまう
クラス変数は原則ネストの外見に行かないがclass << self の中から外の本体のクラスを見に行く


次のプログラムを実行するとどうなりますか

class C
  CONST = "Hello, world"
end

$c = C.new

class D
  class << $c
    def say
      CONST
    end
  end
end

p $c.say

あるクラスのインスタンスの特異クラスのsuperclassは元のクラスになる

class Parent; end
class Child < Parent; end

child_inst = Child.new

# 1. インスタンス(child_inst)の特異クラスの親は？
# => 「元のクラス(Child)」になる
p child_inst.singleton_class.superclass 
# => Child

# 2. クラス(Child)自体の特異クラスの親は？
# => 「親クラス(Parent)の特異クラス」になる
p Child.singleton_class.superclass 
# => #<Class:Parent>

# 3. さらにその親は？
# => 「Parentの親(Object)の特異クラス」になる
p Child.singleton_class.superclass.superclass
# => #<Class:Object>

%w()は式展開しない、Wは式展開する
%iIはシンボル配列、sはシンボル

const_get はselfから定数を探すが「ネスト（外側）」は一切探しに行かずその後継承を探索する

.card {
  position: relative;
  z-index: 1; /* navbar(1030)より低い値 */
}

.main-container {
  position: relative;
  z-index: 1; /* navbarより低い値 */
}
で解決しない


original = ["apple", ["banana"]]
copy = original.dup

copy[0].upcase!
copy[1] << "cherry"
copy << "date"

p original



bootstrapでページ全体を垂直方向中央揃えする
z-indexについて
asdfでパッケージマネージャーをインストールする
JSXについて
Reactの基本的なレンダリングについて
Reactの基本的な状態管理について

Procfileについて
Reactのconsle.logとdebuger

pnpm devの代わりにpnpm buildとpnpm previewを使うことで本番環境での実行が可能になる。
Reactで外部イベントを扱うときはaddEventListenerで登録する


useStateの初期値の設定で型を指定する
ReactはDOMを直接触らない
useRefを使ったDOM操作と宣言的UIの使い分けについて
TypescriptのRecord型について


javasciptのぼっち演算子は?.（undefinedが返る）
scrollIntoView()メソッドでスクロールが可能
refコールバックで要素に対応するDOMノードが引数として渡される
動画再生に関するイベントハンドラ


















































# 全体のゴール
- Zodで「バリデーションルール」を作る
- それをreact-hook-formに渡す
- エラーを画面に出す

---

# 全体戦略（今回も段階的にやる）

1. Zodって何か理解する（超重要）
2. スキーマを作る（まだ連携しない）
3. 型を自動生成する
4. react-hook-formに組み込む
5. エラーを表示する

---

# Step 1: Zodとは何か

## まず考える
- 今は「何でも入力できる」
- でも実際は制限したい

例
- メール形式じゃないとダメ
- パスワードは6文字以上

---

## Zodの役割

- 入力のルールを定義する
- 同時に型も作る

---

## 例

```ts
const schema = z.object({
  email: z.string().email(),
});

意味

emailは文字列
かつメール形式
調べるキーワード
zod object
zod string email
Step 2: スキーマを作る
やること
ログイン用のルールを書く
まず考える（重要）

Swaggerを見て考える

例

メール → 必須 + メール形式
パスワード → 必須 + 6文字以上
仮でいいから書く
import { z } from "zod";

export const loginSchema = z.object({
  mailInput: z.string().email("メール形式で入力してください"),
  passInput: z.string().min(6, "6文字以上で入力してください"),
});
用語説明
z.object
オブジェクトの形を定義する
z.string()
文字列
.email()
メール形式チェック
.min(6)
最低6文字
Step 3: 型を自動生成
なぜやる？
手動で型書くとズレる
書くコード
export type LoginFormValues = z.infer<typeof loginSchema>;
何が起きる？

これと同じになる

type LoginFormValues = {
  mailInput: string;
  passInput: string;
};
メリット
スキーマと型が100%一致する
Step 4: react-hook-formと連携
必要な知識
resolver
resolverとは？
「バリデーションを外部に任せる仕組み」
追加コード
import { zodResolver } from "@hookform/resolvers/zod";
useFormを修正
const { control, handleSubmit } = useForm<LoginFormValues>({
  resolver: zodResolver(loginSchema),
});
なぜこれ？
submit時にZodがチェックするようになる
公式の考え方
resolverで外部バリデーションを接続する

調べるキーワード

react-hook-form resolver zod
Step 5: エラー表示
まず考える
エラーはどこにある？
ヒント

Controllerの引数

render={({ field, fieldState }) => (...)
fieldStateとは？
その入力の状態
中身
fieldState.error
fieldState.invalid
実装
<Controller
  name="mailInput"
  control={control}
  render={({ field, fieldState }) => (
    <TextField
      {...field}
      label="メールアドレス"
      error={fieldState.invalid}
      helperText={fieldState.error?.message}
    />
  )}
/>
なぜこれで表示される？
Zodがエラーを返す
react-hook-formが受け取る
fieldStateに入る
TextFieldに渡す
最終確認
OK条件
間違った入力 → エラー表示
正しい入力 → 通る
理解チェック
Zodは何する？
→ 入力ルールを定義
resolverは何する？
→ react-hook-formとZodをつなぐ
fieldStateは何？
→ エラー情報を持つ
