# til/2025-09-30.md

## 学習内容1
- **タグ**: Ruby, コーディング試験対策
- **タイトル**:メモ
- **内容**:
  - 配列のすべての要素に対し●●する（例：数値に変換）。
    ```
    array.map{|n| n.to_i}
    ```
  - 配列が特定の要素を含むか確認する（例：0を含むか確認）。
    ```
    array.include?(0)
    ```
  - 二次元配列の行列を入れ替える
    ```
    arr2 = arr.transpose
    ```
